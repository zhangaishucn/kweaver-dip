# 找表/找数报告模板（`table_search`）

> 规范见 [`../references/smart-reporting.md`](../references/smart-reporting.md)「A. 找表/找数报告框架」。仅消费 `smart-search-tables` 交付；**不**输出 SQL；表格优先。  
> 找表清单**四列必显**（视图名称、视图 id、所属部门、所属主题）与空值规则见 [`../references/smart-search-tables.md`](../references/smart-search-tables.md) 第 11 步。  
> 总编排：[`../SKILL.md`](../SKILL.md)

**场景**：`table_search`  
**上游**：`smart-search-tables` 最终交付  
**元数据 KN**（若已知）：`[填写 kn_id 或名称]`

---

## 1. 报告摘要

- **用户需求**：[一句话复述]
- **可复核资产条数**：[统计「视图名称 + 视图 id」均非空的条目数；若另列展示技术名称，可再要求技术名齐全] 条

---

## 2. 检索范围与证据来源

| 项目 | 内容 |
| --- | --- |
| 输入来源 | `smart-search-tables` 最终交付 |
| 元数据知识网络 | [如 `metadata` 或实际 kn_id] |
| 检索关键词/二次校验 | [简述；若已做防截断二次校验须写明] |

---

## 3. 候选表/视图清单（可复核资产）

与 [`../references/smart-search-tables.md`](../references/smart-search-tables.md) 第 11 步一致：**固定四列必显**，**所属部门**、**所属主题**无值时单元格**留空**（不用「—」占位，除非交付约定统一占位）。

| 视图名称 | 视图 id | 所属部门 | 所属主题 |
| --- | --- | --- | --- |
| [来自输入，如 business_name] | [来自输入，如 form_view_id] | [来自输入；无则空] | [来自输入；无则空] |

（行数按实际追加；缺「视图名称」或「视图 id」的条目列入「待补充」，不得计入可复核条数。）

**扩展列（可选，另表或同表续列）**：便于问数对齐时，可补充「视图技术名称（表名）」「信息系统」「更新周期」「共享类型」「开放类型」等，**不得**替代上述四列。

| 视图 id（与上表对齐） | 视图技术名称（表名） | 信息系统 | 更新周期 | 共享类型 | 开放类型 |
| --- | --- | --- | --- | --- | --- |
| [同上 form_view_id] | [来自输入] | [来自输入或空] | [来自输入或空] | [来自输入或空] | [来自输入或空] |

---

## 4. 业务含义与适用场景（仅引用输入中的描述）

| 分析场景（简述） | 涉及表（技术名） | 主要字段（来自输入） | 说明（comment/描述原文摘录） |
| --- | --- | --- | --- |
| [场景] | `[technical_name]` | [字段列举] | [禁止臆测口径] |

---

## 5. 治理与职责要点（如有）

| 条目/部门 | 职责要点（摘录） | 证据来源 |
| --- | --- | --- |
| [来自职责查询或「未执行」] | [原文或要点] | [输入片段] |

（若职责未能与单表绑定，写明「通用描述/未能与单表绑定」。）

---

## 6. 下一步建议（不取数版）

- [如何将问题收敛为可 SQL 化的问数，指向已找到表]
- [如需问数，引导 `smart-ask-data` 与业务 kn_id]

---

## 7. 附录：原始输入摘录（可选）

[关键 JSON/字段片段，便于机器或人工复核]

---

## Mermaid（可选）

仅当输入中已有可绑图统计数字时，在**产生该数字的表格紧下方**插入；否则省略。禁止单独文末成章。
