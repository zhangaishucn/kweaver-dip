apiVersion: v1
kind: Service
metadata:
  name: workflow-manage-front
  namespace: {{ .Values.namespace }}
  labels:
    app: workflow-manage-front
spec:
{{- if .Values.service.enableDualStack }}
  ipFamilies:
  - IPv6
  - IPv4
  ipFamilyPolicy: PreferDualStack
{{- end }}
  ports:
    - name: public-port
      port: 1110
      protocol: TCP           
  selector:
    app: workflow-manage-front
  type: {{ .Values.service.type }}
